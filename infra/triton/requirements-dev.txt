# CI-only deps for the infra/triton/tests/ pytest suite. torch + numpy are
# required by test_tmbed_viterbi.py; CPU torch comes from the pytorch index.
--extra-index-url https://download.pytorch.org/whl/cpu
pytest==8.3
numpy==2.1
torch==2.5.1+cpu
