{
  "name": "@protifer/scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "models:build": "python3 build-model-artifact.py --repo protifer-models --org t03i --weights-root ../model-repository",
    "models:guard": "python3 check-model-repository-layout.py"
  },
  "dependencies": {
    "@protifer/shared": "workspace:*",
    "pino": "^9.3.2"
  },
  "devDependencies": {
    "vitest": "^3.0.5"
  }
}
